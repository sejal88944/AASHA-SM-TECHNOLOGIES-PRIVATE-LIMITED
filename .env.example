<<<<<<< HEAD
# Use real MongoDB API (sejal-api): set to 0 and run the API on port 4000
# VITE_CONTACT_MOCK=0

# If your API runs on another port:
# VITE_API_PORT=4000

# Production / custom API URL (full URL; overrides /api/contact)
# VITE_CONTACT_API_URL=https://your-domain.com/api/contact
=======
# Copy to .env or .env.development.local (not committed) and adjust.
# No quotes, no spaces around =. Prefix must be REACT_APP_ for Create React App.

# Local: sejal-api default port
REACT_APP_API_URL=http://localhost:4000

# Production build with a separate API (Render, etc.) — set before npm run build, or in Vercel/Render env:
# REACT_APP_API_URL=https://your-backend.onrender.com
>>>>>>> 6ad3737da72a8b8fb38e5e7a7796410366c6b090
