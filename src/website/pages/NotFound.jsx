import React from 'react';

const NotFound = () => {
    return (
        <div style={{ padding: '80px 5%' }}>
            <h1 style={{ fontSize: '48px', fontWeight: '800', marginBottom: '24px', background: 'linear-gradient(to right, var(--primary), var(--accent))', color: 'var(--primary)', width: 'fit-content' }}>NotFound</h1>
            <p style={{ fontSize: '20px', color: '#4b5563', lineHeight: '1.8', maxWidth: '800px' }}>
                Welcome to the NotFound page. We are crafting a premium experience for you.
            </p>
        </div>
    );
};

export default NotFound;
